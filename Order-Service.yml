
server:
  port: 8080
spring:
  application:
    name: Order-Service
  datasource:
    password: ''
    url: jdbc:h2:mem:testdb
    username: sa
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
